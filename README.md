PaletteVault is a full-stack color palette extraction tool where users upload images and receive optimized color palettes through custom-implemented K-Means clustering algorithms—starting with naive K-Means as a baseline, then implementing K-Means++ for improved initialization, and finally integrating LAB color space for perceptually accurate color selection, with each optimization benchmarked to demonstrate measurable improvements
